import { IonText } from "@ionic/react";

interface ChatProps {
    users: string[];
}

const TempChat: React.FC<ChatProps> = ({users}) => {
    return (
        <></>
    );
}

export default TempChat;